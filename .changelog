
### 历史记录管理优化

**问题描述**：在绘制箭头、椭圆、矩形时，有时会重复触发"历史记录已保存: 初始化"的日志，导致撤销时回到绘制的中间状态。

**根本原因**：

1. `saveState` 被错误地包含在初始化 `useEffect` 的依赖数组中，导致无限循环
2. 绘制过程中的中间状态被意外保存到历史记录
3. 缺少绘制状态管理，无法区分绘制中和绘制完成的状态

**解决方案**：

1. **防重复初始化**：添加 `isInitializedRef` 标志，确保只初始化一次
2. **绘制状态管理**：添加 `isDrawingRef` 标志，防止绘制过程中保存中间状态
3. **事件监听优化**：
   - 监听 `mouse:down` 和 `mouse:up` 事件来跟踪绘制状态
   - 只在绘制完成后保存历史记录
   - 增加适当的延迟确保对象完全添加
4. **依赖管理**：移除可能导致循环的依赖项

**技术实现**：

- 绘制开始时设置 `isDrawingRef.current = true`
- 绘制结束后延迟重置 `isDrawingRef.current = false`
- 在 `saveState` 中检查绘制状态，跳过中间态保存
- 优化事件监听器的时序和延迟机制

**修复文件**：

- `src/tabs/hooks/useHistory.ts` - 重构历史记录管理逻辑
